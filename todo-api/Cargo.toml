[package]
name = "todo-api"
version = "0.1.0"
edition = "2021"

[lints]
workspace = true

[dependencies]
todo-server = { workspace = true, default-features = false }
uuid = { workspace = true }

dioxus = { workspace = true, features = ["fullstack"] }
reqwest = { workspace = true, optional = true }
serde = { workspace = true }

[features]
server = ["dioxus/server", "dep:reqwest"]
