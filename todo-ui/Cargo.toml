[package]
name = "todo-ui"
version = "0.1.0"
edition = "2021"

[lints]
workspace = true

[dependencies]
dioxus = { workspace = true }
todo-api = { workspace = true }

[features]
server = ["todo-api/server"]
