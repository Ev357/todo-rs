[package]
name = "todo-mobile"
version = "0.1.0"
edition = "2021"

[lints]
workspace = true

[dependencies]
dioxus = { workspace = true, features = ["router", "fullstack"] }

[features]
default = []
mobile = ["dioxus/mobile"]
server = ["dioxus/server"]
