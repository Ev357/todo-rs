[package]
name = "web"
version = "0.1.0"
edition = "2021"

[lints]
workspace = true

[dependencies]
dioxus = { workspace = true, features = ["router", "fullstack"] }
ui = { workspace = true }

[features]
default = []
web = ["dioxus/web"]
server = ["dioxus/server", "ui/server"]
